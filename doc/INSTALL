Install OSM-Binary dependencies
	sudo apt-get install -y libprotobuf-dev protobuf-compiler zlib1g-dev libboost-dev ccache astyle \
		libutfcpp-dev

	
Download OSM-Binary (libosmpbf)

	sudo apt-get install -y git
	git clone https://github.com/scrosby/OSM-binary
	cd OSM-Binary
	git checkout v1.3.3
	make -C src 
	sudo make -C src install
