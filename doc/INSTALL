Install LibOsmium dependencies:
   sudo apt-get install \
	cmake cmake-curses-gui make libexpat1-dev zlib1g-dev libbz2-dev \
	libboost-dev libboost-program-options-dev libgdal-dev \
	libgeos++-dev libproj-dev doxygen graphviz libsparsehash-dev

	
Install LibOsmium:
	sudo apt-get install -y git
	cd ~
	git clone https://github.com/osmcode/libosmium/
	cd ~/libosmium
	git checkout v2.5.4

	mkdir build
	cd build
	cmake -DINSTALL_PROTOZERO=ON ..
	make
